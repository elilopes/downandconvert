const fs = require('fs');
let code = fs.readFileSync('server.ts', 'utf-8');

// Replace fetchInstagramDirectFallback and fetchTikTokDirectFallback with a unified one?
// Wait, the user specifically mentioned "se yt-dlp falhar adicione a terceira opção de obter cookies de todas as redes sociais e hospedagem de videos"
